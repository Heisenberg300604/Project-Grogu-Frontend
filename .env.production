# Used by `npm run build` / `npm run start`.
# On Vercel, set NEXT_PUBLIC_API_BASE_URL in the project's environment
# variables instead — that overrides this file.
NEXT_PUBLIC_API_BASE_URL=https://grogu-backend.onrender.com
